require 'spec_helper'

describe TechDevNews do
  it 'has a version number' do
    expect(TechDevNews::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
